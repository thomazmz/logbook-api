{
  "type": "postgres",
  "host": "localhost",
  "port": 5432,
  "username": "logbook",
  "password": "logbok",
  "database": "logbook",
  "synchronize": true,
  "logging": false,
  "entities": [
    "src/domain/**/*.entity.ts"
  ],
  "migrations": [
    "src/infrastructure/typeorm/migrations/**/*.ts"
  ],
  "subscribers": [
    "src/infrastructure/typeorm/subscribers/**/*.ts"
  ],
  "cli": {
    "entitiesDir": "src/infrastructure/typeorm/schemas/**/*.ts",
    "migrationsDir": "src/infrastructure/typeorm/migrations/**/*.ts",
    "subscribersDir": "src/infrastructure/typeorm/subscribers/**/*.ts"
  }
}